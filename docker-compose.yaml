version: '2.4'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000
    environment:
      PYTHONUNBUFFERED: 1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
